/*

        const logs: DailyLog[] = [
                // logs obtained from database
            ];
        
            const logList = logs.map((log: DailyLog, index: number) => (
                <li key={index}>{log.dateCreated}</li>
            ));
        
            
        
        
            const logList = Logs.map(() => (
                <li key={index}>{log.dateCreated} - {log.creator}</li>
            ))
        
            const router = useRouter();
            function handleClick() {
                const navigateToCreateLog = () => {
                    router.push('/createLog')
                }
            }


            <div className="space-y-6">
                <div>
                    <h2 className="text-2xl font-bold tracking-tight">Logs</h2>
                    <button onClick={handleClick}>Create New Log</button>
                </div>

                <div>
                    <ul>{logList}</ul>
                </div>
            </div>
        </DashboardLayout>

*/